# Task: Local Development / Testing

## Pre-requisites

- **MUST** read DevTools Overview: `devdocs/misc/devtools/<DOMAIN>/OVERVIEW.md`

## Directives

- **Check devtools:** Look in `devtools/<DOMAIN>/local/` for:
  - Docker compose files for local environment
  - Justfile with available commands
  - Seed scripts for test data
- **Run Commands:** Use available scripts before creating new ones.

## Workflow

1. Read domain-specific DevTools overview
2. Check existing tools in `devtools/<DOMAIN>/local/`
3. Use existing scripts/commands when available
4. Only create new scripts if no suitable ones exist
